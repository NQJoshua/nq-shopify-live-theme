<div id="allProductsBanner" class="container text-center">
  {% comment %}{% if pbanner_product.jpg != '' %}
  	<img src="{{ 'pbanner_product.jpg' | asset_url }}"/>
  {% endif %} {% endcomment %}
</div><br/><br/>
<h2 class="text-center text-uppercase title-under">{{ settings.pbanner_product_title }}</h2><br/><br/>
{% assign collection = collections.4th-july-1 %}
{% assign pagination_limit_const = settings.pagination_limit | times: 1 %}{% assign supercollection = collection.products %}
{% capture collection_grid %}{% if settings.listing_page_design != "disable-sidebar" and settings.listing_page_design == "left-right-sidebar" %}col-md-6 col-lg-6 col-xl-8{% else %}{% if settings.listing_page_design != "disable-sidebar" and settings.listing_page_design != "closed-sidebar"  %}col-md-8 col-lg-9 col-xl-10{% else %}col-md-12 col-lg-12 col-xl-12{% endif %}{% endif %}{% endcapture %}
{% capture product_grid %}{% case settings.products_in_column %}{% when 'two' %}col-xs-12 col-sm-6 col-md-6 col-lg-6 col-xl-6{% when 'three' %}col-xs-6 col-sm-4 col-md-6 col-lg-4 col-xl-4{% when 'four' %}col-xs-6 col-sm-4 col-md-4 col-lg-3 col-xl-3{% when 'six' %}col-xs-6 col-sm-4 col-md-3 col-lg-2 col-xl-2{% when 'five' %}col-xs-6 col-sm-4 col-md-6 col-lg-4 col-xl-one-fifth{% when 'nine' %}col-xs-6 col-sm-4 col-md-3 col-lg-2 col-xl-one-nine{% else %}{% endcase %}{% endcapture %}

{% paginate collection.products by 1000 %}
{% assign _collectionMax = collection.products %}
{% endpaginate %}
<div class="container collection-page">
  <div class="row">
    <div id="centerCol" class="collection-sale">
      <div class="product-listing row">
        <div class="col-sm-12"><h3>Wrist Wraps</h3></div><br/>
        {% for product in collection.products %}
        	<div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
        {% assign collection = collections.4th-july-2 %}
        {% for product in collection.products %}
        	<div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
        {% assign collection = collections.4th-july-3 %}
        {% for product in collection.products %}
        	<div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
        {% assign collection = collections.4th-july-4 %}
        {% for product in collection.products %}
        	<div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
        {% assign collection = collections.4th-july-5 %}
        {% for product in collection.products %}
        	<div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
        {% assign collection = collections.4th-july-6 %}
        {% for product in collection.products %}
        	<div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
        {% assign collection = collections.4th-july-7 %}
        {% for product in collection.products %}
        	<div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
        {% assign collection = collections.4th-july-8 %}
        {% for product in collection.products %}
        	<div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
        <hr>
      </div>
      <div class="product-listing row">
        <div class="col-sm-12"><h3>Shoes</h3></div><br/>
        {% assign collection = collections.4th-july-9 %}
        {% for product in collection.products %}
        <div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
        <hr>
      </div>
      <div class="product-listing row">
        <div class="col-sm-12"><h3>Gym Gloves</h3></div><br/>
        {% assign collection = collections.4th-july-10 %}
        {% for product in collection.products %}
        <div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
        {% assign collection = collections.4th-july-11 %}
        {% for product in collection.products %}
        <div class="{{ product_grid }}">{% include "product-grid-item-wvariant" hoverzoom: "true" %}</div>
        {% endfor %}
      </div>
    </div>
  </div>
  <hr style="border: 2px solid #d31215; margin-bottom: 100px;">
  <h3 class="text-center text-uppercase">ALL PRODUCTS</h3><br/><br/>
  <div class="row">
    <div id="centerCol" class="collection-sale">
      <div class="product-listing row">
        {% comment %}{% assign more_products = 'elbow-compression-sleeves,wrist-wraps-14-inches,foam-roller-deep-tissue,weightlifting-gloves-with-wrist-support,powerlifting-shoes-megin,super-heavy-duty-wrist-wraps-24-inches,ankle-and-wrist-weights,wrist-wraps-and-lifting-straps-bundle,foam-roller-muscle-massage,lifting-straps-2-pairs,strapwrapz,weightlifting-belt-5,multi-purpose-wrist-wraps-30-inches,weightlifting-gloves-for-women,cross-training-wrist-wraps-32-inches,gymnastic-rings-set' | split:',' %}
        {% for m_product in more_products %}
        	{% for product in collections['all'].products %}
        		{% if product.handle == m_product %}
        			<div class="{{ product_grid }}">{% include "product-grid-item" hoverzoom: "true" %}</div>
        		{% endif %}
        	{% endfor %}
        {% endfor %}{% endcomment %}
        
        {% for product in collections['all'].products %}
        	{% if product.handle != 'kinesiology-tape-primetape-1' %}
          		<div class="{{ product_grid }}">{% include "product-grid-item" hoverzoom: "true" %}</div>
        	{% endif %}
        {% endfor %}
      </div>
    </div>
  </div>
</div>

<script>
  jQuery(window).load(function() {
    $('a[data-pr_id="40295154580"][data-var_id="40295154580"]').trigger('click');
    //$('.knee-sleeves-camo-grey a[data-pr_id="39348891466"][data-var_id="39348892106"]').trigger('click');
    $('a[data-pr_id="40295155540"][data-var_id="40295155540"]').trigger('click');
    //$('.knee-sleeves-black a[data-pr_id="39348891466"][data-var_id="39348891466"]').trigger('click');
  });
</script>

<style>
  .wvariant .product__label--sale.powerlifting-shoes-megin,
  .wvariant .product__label--sale.weightlifting-shoes-venja { display: none !important; }
  /*.weightlifting-gloves .options-swatch--color,
  .knee-sleeves-pink .options-swatch--color,
  .knee-sleeves-camo-grey .options-swatch--color,
  .knee-sleeves-black .options-swatch--color { display: none !important; }*/
  .weightlifting-gloves .options-swatch--color { display: none !important; }
</style>

<script>
  var dtnow = new Date();
  var startDate = new Date();
  var endDate = new Date();
  
    {% for product in collections['all'].products %}
{%- include 'bold-product' with product, hide_action: 'skip' -%}
      {% for collection in product.collections %}
        {% capture sale_tag_spec %}{% include 'get-content-with-key' content: collection.description key: '[sale_tag_spec]' %}{% endcapture %}
        {% assign sale_tag_spec = sale_tag_spec | strip | split: '[/sale_tag_spec]' | first %}
        {% if sale_tag_spec != null %}
             startDate = new Date("{{sale_tag_spec}}");
             endDate = new Date(startDate);                
             endDate.setDate(startDate.getDate() + 15); 
             //endDate = endDate.toISOString().substr(0,10);
  			
  			if(dtnow >= startDate && dtnow <= endDate) {
              jQuery(".product__label--sale." + "{{product.handle}}").css("display","block");  
            }
        {% else %}
            {% capture sale_tag_repeat %}{% include 'get-content-with-key' content: collection.description key: '[sale_tag_repeat]' %}{% endcapture %}
        	{% assign sale_tag_repeat = sale_tag_repeat | strip | split: '[/sale_tag_repeat]' | first %}
            {% if sale_tag_repeat != null %}
  				startDate = "{{sale_tag_repeat}}";
        		if(startDate == "1") {
                  if(dtnow.getDate() >= 1 && dtnow.getDate() <= 15) {
                  	jQuery(".product__label--sale." + "{{product.handle}}").css("display","block");
                  } 
                } else if(startDate == "16") 
                {
                  if(dtnow.getDate() >= 16 && dtnow.getDate() <= 31) {
                  	jQuery(".product__label--sale." + "{{product.handle}}").css("display","block");
                  }
                }
  			{% endif %}
        {% endif %}               
      {% endfor %}
    {% endfor %}
</script>